{{#if products}}
{{#each products}}
<div>
    <h1>{{title}}</h1>
    <h2>{{description}}</h2>
    <p>${{price}} | stock:{{stock}}</p>
    <p>code: {{code}}</p>
    {{#if category}}
    <p>{{category}}</p>
    {{/if}}
    <hr>
</div>
{{/each}}
{{else}}
<h1>No hay productos agregados todavía.</h1>
{{/if}}